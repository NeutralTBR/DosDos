module DosDos

import io
import json
import random

pub struct Dosen {
    externaL_SYSTEM_ID: str
    n_OFFICIAL_NM: str
    descr: str
    descR2: str
    emplid: str
}

fn get_dosen(json_string: str) -> Vec[Dosen] {
    let data: Vec[Dosen] = json.parse(json_string)
    return data
}